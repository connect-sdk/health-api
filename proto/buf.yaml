# prettier-ignore
---
version: v1

name: buf.build/connect-sdk/health-api

deps:
  - buf.build/googleapis/googleapis
  - buf.build/envoyproxy/protoc-gen-validate

lint:
  use:
    - DEFAULT
  ignore:
    - grpc/health/v1/health.proto
  allow_comment_ignores: true
